'use strict';
    let seattle = {
        minCost:23,
        maxCost:65,
        avgCookieSale:6.3,
        arr:[],
        randomNum:function () {
            let random =Math.random
    random=this.minCost*(this.maxCost.minCost+1);
    random=Math.floor(random)
    let numOfcookieBhour=avgCookieSale*random
    return randomNum;
        }
    }
console.log(numOfcookieBhour);

